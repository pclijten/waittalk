{
  "rules": {
    "active_users": {
      "$uid": {
        ".read":  "auth != null",
        ".write": "auth != null && (auth.uid === $uid || data.child('matched').val() === false)"
      }
    }
  }
}
